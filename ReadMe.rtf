 This chat application supports individual chat as well as group chat.
#.To run
1. Run "ServerFrane.java" .
2. Load "data.xml" file in order to start server which contains login credentials of already registered users.
3. Now run "ChatFrame.java" and login with correct credentials which are stored in "data.xml"
4. Select a single user from the friends list in order to start a private chat with a particular user.
5. Select more than one or all users to start a group chat.
}
